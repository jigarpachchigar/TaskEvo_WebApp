import { extendTheme } from "@chakra-ui/react";

// 2. Extend the theme to include custom colors, fonts, etc
const colors = {};
const components = {};

export default theme = extendTheme({ colors, components });
